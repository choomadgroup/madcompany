kind = "web"
previewPath = "/"
title = "Rawon"
version = "1.0.0"
id = "artifacts/rawon"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19525

[services.development]
run = "pnpm --filter @workspace/rawon run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rawon", "run", "build" ]
publicDir = "artifacts/rawon/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19525"
BASE_PATH = "/"
